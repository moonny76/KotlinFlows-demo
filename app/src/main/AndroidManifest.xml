<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="org.scarlet.android.movies.MovieApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Movies">

        <activity
            android:name="org.scarlet.android.currency.livedata.CurrencyActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="true" />
        <activity
            android:name="org.scarlet.android.currency.flow.CurrencyActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="true" />
        <activity
            android:name="org.scarlet.android.password.livedata.PasswordActivity"
            android:exported="true" />
        <activity
            android:name="org.scarlet.android.password.flow.PasswordActivity"
            android:exported="true" />
        <activity
            android:name="org.scarlet.android.collect.SafeCollectActivity"
            android:exported="true" />
        <activity
            android:name="org.scarlet.android.collect.HotFlowActivity"
            android:exported="true" />
        <activity
            android:name="org.scarlet.android.movies.ui.MovieActivity"
            android:exported="true" />
    </application>

</manifest>